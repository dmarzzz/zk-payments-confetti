\* HISTORICAL probe: rev2 close + root rotation at close submission.
\* Expect: PoolSolvency STILL violated (gap-index route survives -- the
\* acceptance predates the close), showing rotation alone is no repair.
CONSTANTS
  Members = {a, z}
  Byz = {z}
  C = 1
  D = 3
  B = 2
  MaxEpoch = 3
  CloseMode = "rev2root"
INIT Init
NEXT Next
INVARIANTS
  TypeOK
  NoOverspend
  NoDoubleAccept
  SlashOnlyOnRealDoubleSpend
  HonestNeverSlashed
  PoolSolvency
  Conservation

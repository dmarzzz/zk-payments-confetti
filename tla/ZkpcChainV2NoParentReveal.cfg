\* Broken mode: unsigned closes exhibit only the opened next-nullifier
\* (PARENT_REVEAL = FALSE), the naive reading Q2(iii) warned about.
\* EXPECTED: BobFloor is violated even with a vigilant Bob - the rollback
\* fork (fresh close of an old parent) admits no evidence. Reproduces the
\* gate rationale for the parent-reveal clause.
SPECIFICATION Spec
CONSTANTS
  D = 2
  MAXLEN = 3
  TABS = 5
  TREQ = 2
  TAU = 2
  TMAX = 7
  VIGILANT = TRUE
  PARENT_REVEAL = FALSE
  CAP_CHECK = TRUE
INVARIANTS
  Conservation
  ForfeitAll
  BobFloor
